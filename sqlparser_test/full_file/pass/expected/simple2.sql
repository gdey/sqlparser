-- This is an sql file with two comments
 -- Also, it's going to contain multiple statements.
 
select a, b from b where b.id in (1, 2, 3) group by b.b ; /* This next one is a bit more complicated. */ 
select a from foo